import React from "react";

import {
  View,
  Text,
  Pressable,
  StyleSheet,
  ActivityIndicator, type ViewStyle, type PressableProps
} from "react-native";
import { colors, typography, spacing, borderRadius, shadows} from '../../screens/themes'

export function Button() {
  return (
    <View>
      <Text>Button</Text>
    </View>
  );
}